# Four Seasons 테니스 복식 대진표

클럽원 5명 + 게스트 5명이 2코트에서 복식을 치는 모임용 정적 웹페이지.
빌드 도구, 프레임워크, 패키지 매니저 없음. HTML 파일 2개가 전부다.

## 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 대진표 페이지. 쿼리스트링으로 명단과 대진을 받아 렌더링만 한다 |
| `setup.html` | 명단 설정 페이지. 이름과 성별 입력 후 "생성"을 누르면 대진을 계산해 대진표 링크를 만든다 |

두 파일은 서로 독립적이며 CSS와 JS를 각각 인라인으로 갖고 있다. 공용 모듈은 없다.
`setup.html`은 기본으로 같은 폴더의 `index.html`을 대진표 주소로 사용한다.

## 배포

- 호스팅: GitHub Pages, `gh-pages` 브랜치의 루트 폴더
- 주소: https://ildukm.github.io/fourseasons/
- `gh-pages`에 푸시하면 1~2분 내 자동 반영된다. 별도 빌드 단계 없음
- 이 저장소의 기본 브랜치는 `gh-pages`다. `main`은 사용하지 않는다
- 확인: `gh api repos/ildukm/fourseasons/pages/builds/latest --jq .status` 가 `built`면 완료

## 명단 전달 규약 (쿼리스트링)

```
index.html?m=클럽원1,...,클럽원5&g=게스트1,...,게스트5&w=m4,g1&s=1839560742824173095647...(100자리)
```

- `m` 클럽원 5명, `g` 게스트 5명. 쉼표 구분, 각각 정확히 5명이어야 한다
- `w` 여성인 사람의 id 목록(`m0`~`m4`, `g0`~`g4`). 없으면 전원 남성으로 본다
- `s` 대진. 라운드당 10자리 × 10라운드 = 100자리 숫자. 각 라운드의 10자리는 순서대로
  1코트 4명(앞 둘 vs 뒤 둘), 2코트 4명, 휴식 2명이며 숫자는 선수 인덱스(0~4 클럽원, 5~9 게스트)다.
  각 라운드 10자리는 0~9의 순열이어야 한다
- 이름에 쉼표는 쓸 수 없다. `setup.html`이 입력 단계에서 막는다
- 파싱 실패 시(`m`/`g` 5명이 아니거나 `s`가 잘못됨) `index.html`은 기본 명단과 기본 대진(`DEFAULT_NAMES`)을 쓰고
  상단에 안내를 띄운다

## 대진 생성 (`setup.html`의 `buildSchedule`)

대진은 `setup.html`에서 "생성"을 누를 때 성별 구성에 맞춰 계산되어 링크(`s`)에 담긴다.
`index.html`은 계산하지 않고 링크의 대진을 그대로 그린다. 그래서 대진표는 즉시 뜨고,
생성 로직을 나중에 바꿔도 이미 공유한 링크의 대진은 바뀌지 않는다.
생성은 `Math.random` 기반 시뮬레이티드 어닐링이라 같은 입력이라도 누를 때마다 다른 대진이 나온다.
데스크톱에서 0.3초, 모바일에서 1초 정도 걸리며 그동안 버튼이 "생성 중…"으로 바뀐다.
생성 결과 아래에는 인원별 남복/혼복/여복 횟수 표(`summaryHtml`)가 나온다. 양 팀 여성 수가 다른 경기가
있을 때만 "기타" 열이 추가된다.

- 10라운드, 2코트. 매 라운드 8명 경기, 2명 휴식. 각자 8게임, 휴식 2회 (휴식 횟수는 구조적으로 고정)
- 내부 표현: `rounds[r]`는 길이 10의 배열로 `s`의 라운드 10자리와 같은 순서다
- `index.html` 렌더링용 `SCHEDULE[r] = { courts: [{ teams: [[pid,pid],[pid,pid]], type }, ...], rest: [pid,pid] }`
- 경기 종류 `type`은 `index.html`이 `w`로 계산한다. 양 팀 여성 수가 같을 때만 `남복`/`혼복`/`여복`.
  다르면 `null`이고 화면에 표시하지 않는다

페널티 가중치(`W`)의 우선순위.

| 조건 | 가중치 | 설명 |
|---|---|---|
| gender | 60 | 양 팀 여성 수 차이. 여성이 있는 경기는 여복 또는 혼복이어야 한다는 조건 |
| partner | 15 | 같은 파트너 반복 |
| rest | 10 | 휴식 간격이 3라운드 미만 |
| mix | 3 | 클럽원끼리 또는 게스트끼리 팀. 약한 선호일 뿐 강제하지 않는다 |
| opp | 1 | 상대 횟수 제곱합. 상대가 고르게 섞이도록 |

- 여성이 홀수면 한 명은 어쩔 수 없이 `혼복`이 아닌 경기(예: 남녀 vs 남남)를 하게 된다. 생성기는 그 횟수를 최소화한다
- 10라운드에서는 "팀은 클럽원+게스트"와 "파트너 반복 금지"를 동시에 만족할 수 없어 파트너 반복 금지를 우선한다
- 라운드 수를 바꾸려면 두 파일의 `R`과 `index.html`의 `s` 검증(100자리), `DEFAULT_NAMES.s`를 함께 바꿔야 한다.
  휴식 초기 배정(`r % 5`)은 라운드 수가 5의 배수일 때만 균등하다

## 클럽원 기본 명단

`setup.html`의 `CLUB_MEMBERS`와 `index.html`의 `DEFAULT_NAMES.m`에 같은 값이 중복 정의되어 있다.
클럽원이 바뀌면 두 곳을 함께 고친다.
성별 기본값은 남이며, `setup.html`의 `CLUB_WOMEN`(이름 기준)과 `index.html`의 `DEFAULT_NAMES.w`(id 기준)에
여성 클럽원이 중복 정의되어 있다. 이것도 함께 고친다.

## 클라이언트 저장소 (localStorage)

| 키 | 위치 | 내용 |
|---|---|---|
| `fs-setup` | setup.html | 마지막으로 생성한 클럽원, 게스트, 여성 id 목록(`w`), 대진표 주소 |
| `fs-me` | index.html | 사용자가 선택한 본인 id (`m0`~`m4`, `g0`~`g4`) |

로컬 파일(`file://`)과 GitHub Pages는 출처가 달라 저장소가 분리된다.
"설정 페이지에 이전 값이 남아 있다"는 문의는 대부분 이 저장값 때문이다.

## 디자인 규칙

- 폰트: 표시용 Barlow Condensed, 본문 Noto Sans KR (Google Fonts)
- 색 의미: 1코트 파랑(`--court1`), 2코트 초록(`--court2`), 선택된 본인 테니스볼 노랑(`--ball`), 휴식 회색(`--rest`)
- 성별 표시: 기본이 남이므로 여성만 `여` 배지(`.f`)로 표시한다. 별도 색 토큰 없이 선 색만 쓴다
- 라이트/다크 테마는 OS 설정(`prefers-color-scheme`)을 따른다. 색 토큰은 `:root`(라이트)와
  `@media (prefers-color-scheme: dark)` 안의 `:root`(다크) 두 곳에 정의하며, 색을 추가할 때 두 곳을 모두 갱신할 것
- 컴포넌트 CSS에는 색 리터럴을 쓰지 않고 토큰(`var(--...)`)만 쓴다
- 모바일 우선. 최대 폭 640px(대진표), 560px(설정)

## 알려진 제약

- 명단이 URL에 그대로 노출된다. Pages는 공개 주소이므로 공유 범위에 주의
